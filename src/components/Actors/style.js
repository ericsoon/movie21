import { makeStyles } from '@mui/styles';

export default makeStyles(() => ({
  poster: {
    maxWidth: '90%',
    borderRadius: '20px',
    boxShadow: '0.5em 0.5em 1em rgb(64,64,70)',
    width: '80%',
  },
}));
